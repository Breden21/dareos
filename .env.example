# Copy this file to .env.local for local development.
# Not used yet — mock data is hardcoded in src/lib/mockData.ts.
# Once the FastAPI backend exists, screens will read this instead:
VITE_API_BASE_URL=http://localhost:8000
